{% extends "HistoireBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} Scenario list {% endblock %}

{% block body %}
<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('scenario_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('scenario_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('scenario_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('scenario_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}
{% extends "UserBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} Joueur creation {% endblock %}

{% block body %}
<form action="{{ path('joueur_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('joueur') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
{% extends "ActionsBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} Actions creation {% endblock %}

{% block body %}

<form action="{{ path('actions_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('actions') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
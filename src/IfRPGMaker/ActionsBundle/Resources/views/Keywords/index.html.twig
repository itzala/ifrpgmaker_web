{% extends "ActionsBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} Keywords list {% endblock %}

{% block body %}
<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Keyword</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('keywords_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.keyword }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('keywords_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('keywords_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('keywords_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}
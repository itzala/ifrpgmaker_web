{% extends "::base.html.twig" %}
{% block title %}{{ parent() }}{% endblock %}
{% block titre %} Syst&egrave;me de jeu {% endblock %}
{% block body %}
{{ parent() }}
    <section>
        <table class="record_properties">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Contenu</th>
                    <td>{{ entity.createur }}</td>
                </tr>
            </tbody>
        </table>

        <ul class="record_actions">
            <li>
                <a href="{{ path('systeme_jeu') }}">
                    Retour
                </a>
            </li>
            <li>
                <a href="{{ path('systeme_jeu_edit', { 'id': entity.id }) }}">
                    Editer
                </a>
            </li>
            <li>
                <form action="{{ path('systeme_jeu_delete', { 'id': entity.id }) }}" method="post">
                    {{ form_widget(delete_form) }}
                    <button type="submit">Supprimer</button>
                </form>
            </li>
        </ul>
</section>
{% endblock %}
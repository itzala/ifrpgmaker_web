{% extends "SystemeJeuBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} SystemeJeu creation {% endblock %}

{% block body %}
<form action="{{ path('systemejeu_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('systemejeu') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
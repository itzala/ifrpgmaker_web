{% extends "SystemeJeuBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} Objet list {% endblock %}

{% block body %}
<table class="records_list">
    <thead>
        <tr>
            <th>Id</th>
            <th>Nom</th>
            <th>Poids</th>
            <th>Encombrement</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
    {% for entity in entities %}
        <tr>
            <td><a href="{{ path('objet_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            <td>{{ entity.nom }}</td>
            <td>{{ entity.poids }}</td>
            <td>{{ entity.encombrement }}</td>
            <td>
                <ul>
                    <li>
                        <a href="{{ path('objet_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('objet_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
            </td>
        </tr>
    {% endfor %}
    </tbody>
</table>

<ul>
    <li>
        <a href="{{ path('objet_new') }}">
            Create a new entry
        </a>
    </li>
</ul>
{% endblock %}
{% extends "SystemeJeuBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} Race creation {% endblock %}

{% block body %}
<form action="{{ path('race_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('race') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
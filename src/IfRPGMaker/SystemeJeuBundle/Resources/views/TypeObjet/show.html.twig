{% extends "::base.html.twig" %}
{% block title %}{{ parent() }} | Caracteristique{% endblock %}
{% block titre %} Caractéristique {% endblock %}
{% block body %}
{{ parent() }}
    <section>        
        <table class="record_properties">
            <tbody>
                <tr>
                    <th>Id</th>
                    <td>{{ entity.id }}</td>
                </tr>
                <tr>
                    <th>Contenu</th>
                    <td>{{ entity.createur }}</td>
                </tr>
            </tbody>
        </table>

        <ul class="record_actions">
            <li>
                <a href="{{ path('caract') }}">
                    Retour
                </a>
            </li>
            <li>
                <a href="{{ path('caract_edit', { 'id': entity.id }) }}">
                    Editer
                </a>
            </li>
            <li>
                <form action="{{ path('caract_delete', { 'id': entity.id }) }}" method="post">
                    {{ form_widget(delete_form) }}
                    <button type="submit">Supprimer</button>
                </form>
            </li>
        </ul>
</section>
{% endblock %}
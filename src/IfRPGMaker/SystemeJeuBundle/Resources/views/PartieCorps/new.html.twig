{% extends "SystemeJeuBundle::base.html.twig" %}

{% block title %}{{parent()}}{% endblock %}

{% block titre %} PartieCorps creation {% endblock %}

{% block body %}
<form action="{{ path('partiecorps_create') }}" method="post" {{ form_enctype(form) }}>
    {{ form_widget(form) }}
    <p>
        <button type="submit">Create</button>
    </p>
</form>

<ul class="record_actions">
    <li>
        <a href="{{ path('partiecorps') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
        <title>{% block title %}IfRPGMaker{% endblock %}</title>
        {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('bundles/site/css/site.css') }}"/>
        {% endblock %}
        <link rel="icon" type="image/x-icon" href="{{ asset('favicon.ico') }}" />
    </head>
    <body>
        {% block body %}                        
            <h1>{% block titre %}{% endblock %}</h1>
            
            {% block menu %}
                <nav>
                    <ul>
                        <li class="item_menu"><a href="{{ path("site_accueil") }}">Accueil</a></li>                        
                        <li class="item_menu"><a class="to_do" href="{{ path("site_accueil") }}">Actions</a></li>
                        <li class="item_menu"><a class="to_do" href="{{ path("site_accueil") }}">Contraintes</a></li>
                        <li class="item_menu"><a class="to_do" href="{{ path("site_accueil") }}">Dialogues</a></li>
                        <li class="item_menu"><a class="doing" href="{{ path("histoire_index") }}">Histoire</a></li>
                        <li class="item_menu"><a class="to_do" href="{{ path("site_accueil") }}">Personnage</a></li>
                        <li class="item_menu"><a class="to_do" href="{{ path("site_accueil") }}">Syst&egrave;me de jeu</a></li>                                                
                        <li class="item_menu"><a class="to_do" href="{{ path("joueurs_index") }}">Joueurs</a></li>                        
                    </ul>        
                </nav>
            {% endblock %}
        
            {% block flash %}
            
            {% for f in app.session.flashbag %}
                    <div>
                        {{ dump(f) }}
                    </div>
                {% endfor %}
            {% endblock %}
            
        {% endblock %}
        
        {% block footer %}
            <footer>
                <span>&copy;</span> Tous droits r&eacute;serv&eacute;s
            </footer>            
        {% endblock %}
        
        {% block javascripts %}
            <script type="text/javascript" src="{{asset("bundles/site/js/jquery.js")}}"></script>
            <script type="text/javascript">
                $().ready(function(){
                    $(".to_do").click(function(event){
                        event.preventDefault();
                        alert("Merci de bien vouloir vérifier les routes du bundle. Ensuite, éditez le fichier /app/Resources/views/base.html.twig et supprimez l'attribut class du lien");
                    })
                    $(".doing").click(function(){                        
                        alert("Partie du site en construction :)");
                    })
                });                
            </script>
        {% endblock %}
    </body>
</html>
